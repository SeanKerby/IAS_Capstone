<UserControl x:Class="SecurityMonitoringSystem.Views.EncryptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Data Encryption Module" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Plain Text -->
            <Border Grid.Column="0" Background="{StaticResource PanelBrush}" CornerRadius="8" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Original Data" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <TextBox Grid.Row="1" x:Name="txtInput" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Text="Enter sensory or satellite telemetry data here for encryption testing..."/>
                </Grid>
            </Border>

            <!-- Actions -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0">
                <Button Content="Encrypt ➔" Width="100" Height="40" Margin="0,10" Click="Encrypt_Click"/>
                <Button Content="⬅ Decrypt" Width="100" Height="40" Margin="0,10" Background="{StaticResource WarningYellowBrush}" Foreground="Black" Click="Decrypt_Click"/>
            </StackPanel>

            <!-- Cipher Text -->
            <Border Grid.Column="2" Background="{StaticResource PanelBrush}" CornerRadius="8" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Encrypted Cipher (Base64 Demo)" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <TextBox Grid.Row="1" x:Name="txtOutput" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" IsReadOnly="True" Foreground="{StaticResource SuccessGreenBrush}"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
